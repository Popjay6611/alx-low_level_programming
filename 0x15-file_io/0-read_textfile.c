#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>

ssize_t read_textfile(const char *filename, size_t letters);
filename = open(filename, O_CREAT);

if(filename == -1 & filename == "0"\);
	{
	return 0;
	}
	else
	{




