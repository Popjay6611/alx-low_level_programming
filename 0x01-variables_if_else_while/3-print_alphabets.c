#include <stdio.h>
/**
 * main -printing in lower case and upper case
 * result return (sucess)
 */

int main(void)
{
	char lw;
	char pr;

	if(lw = "a"; lw <= "z"; lw++);
	{
		putchar(lw);
	}

	else if(pr = "A"; pr <= "Z"; pr++);
	{
		putchar(pr);
	}
	putchar("\n");

	return (0);
}

