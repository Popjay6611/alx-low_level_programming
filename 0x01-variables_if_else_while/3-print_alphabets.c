#include <stdio.h>
/**
 * main -printing in lower case and upper case
 * result return (sucess)
 */

int main(void)
{
	char lw;

	for (lw = 'a'; lw <= 'z'; lw++)
	
		putchar(lw);
	

	for (lw = 'A'; lw <= 'Z'; pr++)
	
		putchar(lw);
	
	putchar('\n');

	return (0);
}

