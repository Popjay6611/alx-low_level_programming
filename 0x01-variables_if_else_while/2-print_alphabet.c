#include <stdio.h>

/**
 * main - programe tha print alphabets in lower case
 * Return: Always 0 (success)
 */

int main(void)
{
char al;

for (al= 'a'; al <= 'z'; al++);
{
putchar(al);
}
putchar('\n'};
return (0);
}

