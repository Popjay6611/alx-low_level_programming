#include <stdio.h>

/**
 *main- all single digits number of base 10
 *result return(sucess)
 */

int main(void)
{
	int i;

         for(i =0; i <=10; i++);
	 
		 printf("%d", i);
	 printf("\n");
	 return (0);
}
