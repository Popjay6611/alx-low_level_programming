#include "main.h"
#include <stdio.h>

/**
 *main- compute the absolute value of an integer
 *Return: Always 0 (success)
 */

int _abs(int y)
{
 if (y < 0)
	 y = y * -1
		 return y;
 else
	 return y;
}
